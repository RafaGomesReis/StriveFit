
import { Exercicio } from "../types/exercise";

export const getExercises = () => {
    // return data.exercises;
}
export const getExerciseById = (id: string) => {
    // return data.exercises.find((exercise) => exercise.id === id);
}
export const getExercisesByList = (listId: string) => {
    // return data.exercises.filter((exercise) => exercise.listId === listId);
}